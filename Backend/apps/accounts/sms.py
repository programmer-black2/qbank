def sendSMS(phone, message):
    """
    SMS delivery contract.

    The real Faraz SMS implementation will be added later. For now, callers
    should use this function whenever an SMS must be sent.
    """
    return True
